# Cycle charts (Visual Chart)

This folder contains Visual Chart screenshots used to illustrate the bull/bear cycle segmentation produced by the rule-based extrema method.

- `cycle_1.gif`, `cycle_2.gif`, `cycle_3.gif`: example cycles (annotated with duration and % move).
- These figures correspond to the cycle definitions summarized in the Excel workbook in the repository root.

